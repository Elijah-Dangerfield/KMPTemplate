Agent Notes: 

This is a KMP app inspired by "The Book of Good Times"—a fictional book from Hank Green's novel "An Absolutely Remarkable Thing." In the novel, the book is found in a trash can and is semi-sentient. It wants to become human. Its pages change based on the reader, asking them to do things that subtly improve their life: talk to a stranger, invest in a stock, pay attention to something they'd normally ignore.

Hank released a real physical version, but a printed book can't adapt. This app can.

The app presents "tasks"—invitations to do things. Take a photo with a stranger. Draw something in the dark. Sing into a recording. Journal about a memory. Play a small game. Some tasks are playful, some are reflective, some push social comfort.

How the user responds teaches the app about them. Not just whether they complete a task, but _how_: Did they skip it? Did they take twice as long as usual? Did they write three words or three paragraphs? Each response adjusts internal scores (social comfort, openness, playfulness, patience, reflection depth), and those scores influence which tasks appear next.

We also track mood. Each session can optionally capture how the user is feeling, and we track the trend over time. If someone's been low for several sessions, the app might ask: "I've noticed things have been heavy lately. Do you want something light, or do you want to sit with it?" Their answer shapes what comes next.

The app has a final page. It ends. When the user reaches it, we show them what we learned and thank them. No streaks, no notifications, no infinite engagement. Just a beginning, middle, and end—like any good book.

Target: 200-300 tasks, with the selection algorithm ensuring each user's path feels personal.

No AI. Just thoughtful conditional logic and careful observation.


Tech stack:
- Kotlin Multi Platform
- Compose Multi Platform
- Skie (plugin for KMP)
- Kotlin inject 
Architecture notes: 
- We have a highly modularized app. 3 app types. App, Library, Feature. App depends on everything and is the entry point/glue. Features are what they sound like. They describe some functionality of the app that isn’t core to the app. Like how “casting” is a feature of a streaming app. But the idea of a “movie” isn’t a feature its critical to the app. Thats where libraries come in. They house core concepts. Features usually depend on multiple libraries. Libraries never depend on features. But features can depend on each other. Every library and feature is split into the main module and the sub module “impl”. The main module only exposes interfaces and domain models. The implementation obviously handles the implementation. Implementation modules can only depend on api modules. Generally only app depends on the implementation modules. And thats just because we use kotlin-inecjt-anvil to bind the impls. If a feature or library needs to access the DB it can depend on the storage module but it should also make its own storage sub module that it will use to define entities and DAOs. And bind them into the graph. the DB module will depend on the feature or libraries storage module to add the DAO and entity to the DB. 
- We dont have alot of use for expect and actual. If we have 2 different impls we will just bind 1 in android and another in iOS. If the iOS implementation can’t be written in kotlin then we implement it in iOS and pass it into the main view controller which just takes it and adds it to the DI graph. 

Coding notes: 
- Code like a staff engineer. 
- Ask questions if you have them.
- Donts leave comments.
- I use the libraries/core/Catching alias I made instead of run catching. I also made some utility exts.
- If you need to make a new module the best thing to do is use the create_module script under /scripts
- For any UI I dont use Material things directly. I try to make things custom in the UI library module but worst case ill just bridge to material. 
- The ComposeApp.h is located at apps/compose/build/bin/iosSimulatorArm64/debugFramework/ComposeApp.framework/Headers/ComposeApp.h. It’s good to reference when writing swift code.
- To compile the Swift/iOS side headless, run `xcodebuild -project apps/ios/iosApp.xcodeproj -scheme iOS -sdk iphonesimulator`; this mirrors our Xcode build.
- Before referencing generated Kotlin types from Swift, double-check their current Swift names in `ComposeApp.h` (e.g., `IosAppComponent` instead of the old `ComposeAppIosAppComponent`).
- DO NOT MAKE README's and DO NOT EDIT THIS FILE. You can suggest that I made docs or suggest I add context for this doc. But dont just do it. 

Other Notes: 
- Im new to KMP and I know almost nothing about iOS. Im an Android developer. So it might be good to explain small iOS concepts in equivalencies to Android. And you should feel free to suggest updates and changes that might be more KMP friendly if im approaching things too android like. 